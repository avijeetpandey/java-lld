## Expense policy rule engine

Employees submit thousands of expenses daily (flights, meals, cabs). A finance team cannot manually review all of them. The company needs a system that automatically evaluates an expense against a set of company policies to determine if it should be `APPROVED`, `REJECTED`, or flagged for `MANUAL_REVIEW`.